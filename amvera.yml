version: "2"
meta:
  name: "ophthalmology-hospital"
  environment: "docker"
  toolchain:
    name: "docker"
    version: "latest"
build:
  dockerfile: "Dockerfile" 
run:
  persistence:
    - mountPath: "/app/static/uploads"
      name: "storage"
  ports:
    - name: "http"
      port: 8000
